<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hupays_nenich.com.sms114" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".AccueilActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".mesInfos.MesInfosActivity"
            android:label="@string/title_activity_mes_infos" >
        </activity>
        <activity
            android:name=".causes.CauseActivity"
            android:label="@string/title_activity_cause" >
        </activity>
        <activity
            android:name=".causes.DetailAccidentVoitureActivity"
            android:label="@string/title_activity_detail_accident_voiture" >
        </activity>
        <activity
            android:name=".causes.DetailIncendieActivity"
            android:label="@string/title_activity_detail_incendie" >
        </activity>
        <activity
            android:name=".causes.DetailAnimauxActivity"
            android:label="@string/title_activity_detail_animaux" >
        </activity>
    </application>

</manifest>
